import { ImageDeleteDirective } from './image-delete.directive';

describe('ImageDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
