import { NgImageUploadDirective } from './ng-image-upload.directive';

describe('NgImageUploadDirective', () => {
  it('should create an instance', () => {
    const directive = new NgImageUploadDirective();
    expect(directive).toBeTruthy();
  });
});
