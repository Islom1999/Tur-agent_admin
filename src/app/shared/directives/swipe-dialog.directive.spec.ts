import { SwipeDialogDirective } from './swipe-dialog.directive';

describe('SwipeDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new SwipeDialogDirective();
    expect(directive).toBeTruthy();
  });
});
